# AI Scientist MVP environment template (T001).
# Variable names and safe placeholder values ONLY. The offline Replay needs
# no real keys. Copy this file to a local .env only when a later task
# authorizes a Live provider; .env is never committed.

# Local live Qwen-VL calls only; keep the real value in ai_scientist-main/.env.
DASHSCOPE_API_KEY=change_me
DEEP_RESEARCH_API_KEY=change_me
JSOC_API_TOKEN=change_me
