# D-006 项目所有者回复附录

> 记录状态：`IMMUTABLE_RESPONSE_EVIDENCE`
>
> 决策日期：2026-08-19
>
> 对应请求：`D-006`

## 1. 原始回复

```text
D-006=A
actor_id=project_owner_01
补充意见=无
```

## 2. 规范化解释

项目所有者选择方案 A：首个 MVP 采用一个聚合 `RunReadModel`，将运行、阶段、领域摘要、Artifact、Finding、闸门、血缘和报告摘要组织为稳定的后端查询投影；原始载荷和全文通过引用式详情端点访问。

该选择不改变底层 Artifact、科学语义或审批边界。Read Model 是由不可变 Artifact、Ledger 事件、ValidationReport、Finding 和 Decision 投影出的查询视图，不是新的科学事实源。

## 3. 执行边界

- 首屏只读取小型摘要和绑定引用，不读取 LangGraph State、绝对路径或完整 Markdown/CSV/图片字节；
- `authorization_status`、`scientific_verdict`、`result_maturity`、`release_scope` 分开呈现；
- S04 默认仍只展示 0814 运行投影，0808 只由 provenance/审计详情访问；
- 前端不得自行计算 S05/S06 汇合、Finding 阻断或科学结论；这些状态由后端投影器产生；
- Read Model 版本变化必须生成新的 Schema/投影版本，不回写上游 Artifact。
