# TASK-P5-06：JW-SSD 批量运行预算与故障说明

> 状态：`COMPLETED`

已在推理入口增加完整 Qwen 运行的显式 `--confirm-batch` 闸门；未提供该参数时只能运行 1–4 条 pilot。配置固定最多 195 次请求、单请求 180 秒、最多 3 次重试、指数退避上限 16 秒、最小间隔 0.2 秒和 200 CNY 成本上限。批量耗时按请求数、重试和服务端延迟实测记录，不在未执行时伪造估算。详见 `docs/JWSSD_BATCH_RUNBOOK.md`。
