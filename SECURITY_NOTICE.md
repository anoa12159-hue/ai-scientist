# 安全处置通知

日期：2026-08-25

## 已发现风险

根目录 `deep_research_agent_v13.zip` 内含 `deep_research_agent_v13/.env`，其中
`S2_API_KEY` 为非空、非明显占位值。该凭证值后来在沟通渠道中被直接披露，因此无论此前
是否被使用，都必须按已泄露凭证处置。项目文档和 Git 历史不得记录其值。

## 当前处置

- 原压缩包作为用户提供的只读移交证据保留，未覆盖、删除或重新打包。
- 该压缩包不得进入公开仓库、发布包、镜像或附件。
- 后续接收源码时只提取代码和允许的缓存，不提取 `.env`、`.venv` 或真实凭证。
- `S2_API_KEY` 按 v13 的检索/引用链上下文理解为 Semantic Scholar（S2）服务凭证，不是
  当前 Qwen/DashScope 模型凭证；最终服务归属仍应由脱敏源码或密钥所有者确认。

## 所需人工动作

1. 由密钥所有者立即吊销或轮换原 `S2_API_KEY`。
2. 确认旧密钥失效后，只通过环境变量或受控 Secret Store 注入新密钥。
3. 发布前运行凭证扫描，并检查压缩包、Git 历史、日志和缓存。

本项目不会记录、复制或尝试使用该密钥。
